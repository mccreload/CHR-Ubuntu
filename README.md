# CHR-Ubuntu
MikroTik CHR Installer for Ubuntu VPS
sudo su
git clone https://github.com/mccreload/CHR-Ubuntu
cd CHR-Ubuntu
chmod +x install.sh
sudo apt-get install dos2unix
dos2unix install.sh
bash install.sh
